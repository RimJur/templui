package showcase

import (
	"github.com/RimJur/templui/components/button"
	"github.com/RimJur/templui/components/tooltip"
)

templ TooltipDefault() {
	@tooltip.Tooltip() {
		@tooltip.Trigger(tooltip.TriggerProps{}) {
			@button.Button(button.Props{
				Variant: button.VariantOutline,
			}) {
				Hover Me
			}
		}
		@tooltip.Content(tooltip.ContentProps{
			ID:       "tooltip-default",
			Position: tooltip.PositionTop,
		}) {
			Add to cart
		}
	}
}
